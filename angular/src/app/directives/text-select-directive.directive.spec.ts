import { TextSelectDirectiveDirective } from './text-select-directive.directive';

describe('TextSelectDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSelectDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
